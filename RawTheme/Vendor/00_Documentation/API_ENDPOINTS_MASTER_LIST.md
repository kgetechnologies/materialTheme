# API Endpoints Master List

This document will serve as a master index of all API endpoints covered by the UI mockups and documentation. For each feature, see the corresponding API `.md` file in the relevant subfolder for details.

---

## Example (fill in as needed):

- **Products**: `/api/v1/VendorClient/Products/{companyId}`
- **OrderLines**: `/api/v1/VendorClient/OrderLines/{companyId}`
- **Users**: `/api/v1/VendorClient/Users/{companyId}`

(For a full list, see each feature's API documentation in the subfolders.)
